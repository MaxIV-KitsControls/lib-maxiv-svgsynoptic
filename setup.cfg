[bdist_rpm]

build_requires = python
                 python-setuptools

requires = python-fandango
           python-taurus
           python-pytango
           tangods-pyalarm
           qtwebkit

release = 1%{?dist}.maxlab
